<UserControl x:Class="YC.WalletApp.Domain.PartViewControl.DataGridPaginationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:YC.WalletApp.Extension"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local:BoolToYesNoConverter x:Key="BoolConverter"/>
        <local:LessThanConverter x:Key="LessThanConverter"/>
        <local:GreaterThanConverter x:Key="GreaterThanConverter"/>
        <Style x:Key="TooltipCellStyle" TargetType="TextBlock">
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <FrameworkElement x:Name="DataContextProxy" DataContext="{Binding}" />
        <!-- 数据表格 -->
        <DataGrid x:Name="MainDataGrid" 
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Items}"
                  SelectionUnit="Cell"
                  Grid.Row="0">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                          EditingElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnEditingStyle}"
                          ElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnStyle}">
                    <DataGridCheckBoxColumn.Header>
                        <Border Background="Transparent">
                            <CheckBox IsChecked="{Binding DataContext.IsAllSelected, Source={x:Reference DataContextProxy}}" />
                        </Border>
                    </DataGridCheckBoxColumn.Header>

                    <DataGridCheckBoxColumn.HeaderStyle>
                        <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                        </Style>
                    </DataGridCheckBoxColumn.HeaderStyle>
                </DataGridCheckBoxColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 分页控件 -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1" Margin="0 10">
            <ComboBox ItemsSource="{Binding PageSizes}" 
                     SelectedValue="{Binding PageSize}"
                     Width="80" Margin="0,0,10,0"/>

            <Button Content="上一页" Command="{Binding PreviousPageCommand}"
                   IsEnabled="{Binding CanPrevious}"/>

            <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" Margin="5,0"/>
            <TextBlock Text="/" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center"/>

            <TextBox Text="{Binding TargetPage}" Width="50" Margin="10,0"
                    VerticalContentAlignment="Center"/>
            <Button Content="跳转" Command="{Binding GoToPageCommand}" Margin="5,0"/>

            <Button Content="下一页" Command="{Binding NextPageCommand}"
                   IsEnabled="{Binding CanNext}"/>
        </StackPanel>
    </Grid>
</UserControl>
